{
  "name": "plumcodes-deriv-multibot",
  "version": "1.0.0",
  "description": "Orquestrador 24/7 de multiplos bots de trading na Deriv (API WebSocket oficial)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --env-file=.env src/index.ts",
    "dev": "node --env-file=.env --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.18"
  }
}
